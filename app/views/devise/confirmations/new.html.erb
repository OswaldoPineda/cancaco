<div class="container col-11 col-md-8 col-lg-5 bg-light border rounded px-3 px-md-5 pb-5 pt-3 mt-3 mt-md-5 text-form">
  <div class="my-1 d-flex justify-content-center">
    <%= link_to root_path do %>
      <%= image_tag 'canaco-logo.png', size: '160x110' %>
    <% end %>
  </div>
  <h3 class="col-12 text-left my-3 px-0 subtitle">Reenviar confirmación de cuenta</h3>
  <p class="taxt-left text-muted">
    Te reenviaremos nuevamente un correo con las instrucciones
    para reestablecer tu contraseña.
  </p>
  <%= form_for(resource,
               as: resource_name,
               url: confirmation_path(resource_name),
               html: { class: "d-flex flex-column align-items-center", method: :post }) do |f| %>
    <% @error_messages = resource.errors.messages %>

    <div class="field w-100">
      <%= f.label :email %><br>
      <%= f.email_field :email,
                        autofocus: true,
                        autocomplete: "email",
                        value: (
                          resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email
                        ),
                        class: "form-control" %>
      <div class="invalid-feedback">
        <%= invalid_feedback_message(messages: @error_messages, field: :email) %>
      </div>
    </div>

    <br>
    <div class="actions w-100">
      <%= f.submit "Reenviar confirmación", class: "btn btn-block primary-button" %>
    </div>
    <div class="row justify-content-between w-100">
      <%= link_to "Iniciar Sesión", new_session_path(resource_name) %>
      <%= link_to "Regístrate", new_registration_path(resource_name) %>
    </div>
  <% end %>
</div>

<div class="container my-5">
  <div class="row justify-content-center">
    <div class="col-md-8 border-top border-dark">
      <div class="row mt-3 justify-content-around">
        <a href="#">Condiciones de uso</a>
        <a href="#">Aviso de privacidad</a>
        <a href="#">Ayuda</a>
      </div>
      <div class="row mt-3">
        <div class="col">
          <p class="text-center font-weight-light">&copy; 1990-2019, CANACO o afiliados. Todos los derechos reservados</p>
        </div>
      </div>
    </div>
  </div>
</div>
